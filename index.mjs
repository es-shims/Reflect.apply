import getPolyfill from 'reflect.apply/polyfill';

export default getPolyfill();

export { default as getPolyfill } from 'reflect.apply/polyfill';
export { default as implementation } from 'reflect.apply/implementation';
export { default as shim } from 'reflect.apply/shim';
